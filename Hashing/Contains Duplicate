class Solution {
public:
    bool containsDuplicate(vector<int>& nums) 
    {
        unordered_set<int> sett;
        for(auto& ele:nums)
        {
            if(sett.count(ele)) return true;
            sett.insert(ele);
        }
        return false;
    }
};
